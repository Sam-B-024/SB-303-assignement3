#ifndef RECURSIVE_H
#define RECURSIVE_H

#include <vector>

// Declaration function to find last occurrence of a target value in a vector.
// Returns the index of last occurrence of target in vector, or -1 if not found.
int findLastOccurrence(const std::vector<int>& vec, int target);

#endif // RECURSIVE_H